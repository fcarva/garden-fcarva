{
  "name": "garden-fcarva",
  "description": "Fcarva's Digital Garden",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd quartz && npm install && npm run quartz -- build",
    "dev": "cd quartz && npm run quartz -- build --serve"
  },
  "engines": {
    "node": ">=18"
  }
}
