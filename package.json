{
  "name": "garden-fcarva",
  "description": "Fcarva's Digital Garden",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd quartz && npm install && node quartz/bootstrap-cli.mjs build",
    "dev": "cd quartz && npm install && node quartz/bootstrap-cli.mjs build --serve"
  },
  "engines": {
    "node": ">=22"
  }
}
